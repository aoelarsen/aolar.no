---

---

<style lang="scss">
  .header {
    width: 100%;
    padding: 1rem;
    align-self: center;
    &__title {
      padding: 0.5rem;
      font-size: clamp(1.5rem, calc(1.5rem + 1.3vw), 3rem);
    }
    &__text {
      font-size: 1.1rem;
      padding-left: 5%;
    }
  }
</style>

<header class="header" id="header">
  <slot />
</header>